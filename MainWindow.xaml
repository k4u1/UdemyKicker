<ui:FluentWindow x:Class="UdemyKickerWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        mc:Ignorable="d"
        Title="UdemyKicker - WPF" Height="720" Width="1280"
        WindowStartupLocation="CenterScreen"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="None"
        Background="#0C1310"
        Loaded="Window_Loaded">

    <Grid Background="#0C1310">
        <!-- Main Layout Grid -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="220"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Sidebar with Rounded Left Corners -->
            <Border x:Name="SidebarBorder" Grid.Column="0" Background="#141916" BorderBrush="#1AFFFFFF" BorderThickness="0,0,1,0" CornerRadius="16,0,0,16">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Sidebar Header (Anime Profile Banner with Rounded Top Corners) -->
                    <Border Grid.Row="0" Height="140" Margin="0,0,0,12" CornerRadius="16,16,0,0" ClipToBounds="True">
                        <Grid>
                            <Image Source="yhwach.png" Stretch="UniformToFill" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            
                            <!-- Gradient Overlay for Text Readability -->
                            <Border>
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                        <GradientStop Color="#00000000" Offset="0.4"/>
                                        <GradientStop Color="#CC141916" Offset="1.0"/>
                                    </LinearGradientBrush>
                                </Border.Background>
                            </Border>

                            <!-- Text Info -->
                            <StackPanel VerticalAlignment="Bottom" Margin="16,0,16,12">
                                <TextBlock Text="@K4uku" FontSize="18" FontWeight="Bold" Foreground="White">
                                    <TextBlock.Effect>
                                        <DropShadowEffect Color="Black" BlurRadius="4" ShadowDepth="1" Opacity="0.8"/>
                                    </TextBlock.Effect>
                                </TextBlock>
                                <TextBlock Text="Tool owner" FontSize="11" Foreground="#4edea3" FontWeight="SemiBold">
                                    <TextBlock.Effect>
                                        <DropShadowEffect Color="Black" BlurRadius="4" ShadowDepth="1" Opacity="0.8"/>
                                    </TextBlock.Effect>
                                </TextBlock>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- Sidebar Menu -->
                    <StackPanel x:Name="SidebarStackPanel" Grid.Row="1" Margin="8,0">

                        <Button Tag="Home" Content="Home" Click="NavItem_Click" Background="Transparent" Foreground="#bbcabf" BorderThickness="0" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Padding="16,12" FontSize="16">
                            <Button.ContentTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <ui:SymbolIcon Symbol="Home24" Margin="0,0,12,0"/>
                                        <TextBlock Text="{Binding}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </Button.ContentTemplate>
                        </Button>

                        <Button Tag="Library" Content="Downloads" Click="NavItem_Click" Background="Transparent" Foreground="#bbcabf" BorderThickness="0" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Padding="16,12" FontSize="16">
                            <Button.ContentTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <ui:SymbolIcon Symbol="ArrowDownload24" Margin="0,0,12,0"/>
                                        <TextBlock Text="{Binding}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </Button.ContentTemplate>
                        </Button>

                        <Button Tag="Browser" Content="Browser/API" Click="NavItem_Click" Background="Transparent" Foreground="#bbcabf" BorderThickness="0" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Padding="16,12" FontSize="16">
                            <Button.ContentTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <ui:SymbolIcon Symbol="Globe24" Margin="0,0,12,0"/>
                                        <TextBlock Text="{Binding}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </Button.ContentTemplate>
                        </Button>

                        <Button Tag="Logger" Content="App Logs" Click="NavItem_Click" Background="Transparent" Foreground="#bbcabf" BorderThickness="0" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Padding="16,12" FontSize="16">
                            <Button.ContentTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <ui:SymbolIcon Symbol="Document24" Margin="0,0,12,0"/>
                                        <TextBlock Text="{Binding}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </Button.ContentTemplate>
                        </Button>

                        <Button Tag="Translator" Content="Translator" Click="NavItem_Click" Background="Transparent" Foreground="#bbcabf" BorderThickness="0" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Padding="16,12" FontSize="16">
                            <Button.ContentTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <ui:SymbolIcon Symbol="LocalLanguage24" Margin="0,0,12,0"/>
                                        <TextBlock Text="{Binding}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </Button.ContentTemplate>
                        </Button>

                        <Button Tag="Player" Content="Player" Click="NavItem_Click" Background="Transparent" Foreground="#bbcabf" BorderThickness="0" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Padding="16,12" FontSize="16">
                            <Button.ContentTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <ui:SymbolIcon Symbol="Play24" Margin="0,0,12,0"/>
                                        <TextBlock Text="{Binding}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </Button.ContentTemplate>
                        </Button>

                        <Button Tag="Settings" Content="Settings" Click="NavItem_Click" Background="Transparent" Foreground="#bbcabf" BorderThickness="0" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Padding="16,12" FontSize="16">
                            <Button.ContentTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <ui:SymbolIcon Symbol="Settings24" Margin="0,0,12,0"/>
                                        <TextBlock Text="{Binding}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </Button.ContentTemplate>
                        </Button>

                    </StackPanel>

                    <!-- Sidebar Footer (Log Out) -->
                    <StackPanel Grid.Row="2" Margin="8,0,8,20">
                        <Button Tag="Logout" Content="Log Out" Click="NavItem_Click" Background="Transparent" Foreground="#bbcabf" BorderThickness="0" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Padding="16,12" FontSize="16">
                            <Button.ContentTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <ui:SymbolIcon Symbol="SignOut24" Margin="0,0,12,0"/>
                                        <TextBlock Text="{Binding}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </Button.ContentTemplate>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Main Content Area Container -->
            <Grid x:Name="MainContentAreaGrid" Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Top License Header -->
                <!-- Padded 140px on the right to prevent overlap with Windows system buttons (Min, Max, Close) -->
                <Grid x:Name="MainTitleBar" Grid.Row="0" Margin="24,12,140,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="UdemyKicker" FontSize="26" FontWeight="Bold" Foreground="#4edea3" Margin="0,0,16,0">
                            <TextBlock.Effect>
                                <DropShadowEffect Color="#4edea3" BlurRadius="12" ShadowDepth="0" Opacity="0.8"/>
                            </TextBlock.Effect>
                        </TextBlock>
                        <Ellipse x:Name="StatusDot" Width="10" Height="10" Fill="#4edea3" VerticalAlignment="Center" Margin="0,0,8,0">
                            <Ellipse.Effect>
                                <DropShadowEffect Color="#4edea3" BlurRadius="8" ShadowDepth="0" Opacity="0.8"/>
                            </Ellipse.Effect>
                        </Ellipse>
                        <TextBlock x:Name="StatusText" Text="License Active" FontSize="14" Foreground="#9DB0A6" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    </StackPanel>

                    <TextBlock Grid.Column="1" x:Name="TimeRemainingText" Text="48 Days and 03:15:40" FontSize="15" FontWeight="Bold" Foreground="#bbcabf" VerticalAlignment="Center"/>
                </Grid>

                <!-- Floating Rounded Content Card -->
                <Border Grid.Row="1" Background="#111A15" BorderBrush="#1AFFFFFF" BorderThickness="1" CornerRadius="16" Margin="12,0,16,16" ClipToBounds="True">
                    <Grid>
                        <Frame x:Name="RootFrame" NavigationUIVisibility="Hidden"/>
                        <wv2:WebView2 x:Name="webView" Visibility="Collapsed" Width="0" Height="0"/>
                    </Grid>
                </Border>
            </Grid>
        </Grid>

        <!-- Transparent TitleBar overlayed at the top of the entire window for window dragging and control buttons -->
        <ui:TitleBar Title="" Background="Transparent" Foreground="White" Height="40" VerticalAlignment="Top"/>
    </Grid>
</ui:FluentWindow>